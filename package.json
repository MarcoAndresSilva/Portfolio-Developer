{
  "name": "portfolio-developer",
  "version": "0.0.0",
  "description": "Portafolio de desarrollador de Marco Andrés Silva. Angular (SSG) + NestJS, monorepo con npm workspaces.",
  "private": true,
  "author": "Marco Andrés Silva <marco.silvaponce10@gmail.com>",
  "license": "MIT",
  "engines": {
    "node": ">=22.22.3"
  },
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "scripts": {
    "web": "npm run start --workspace apps/web",
    "api": "npm run start:dev --workspace apps/api",
    "typecheck": "npm run typecheck --workspace libs/shared && npm run typecheck --workspace apps/api",
    "lint": "npm run lint --workspace apps/api",
    "test": "npm run test --workspace apps/web && npm run test --workspace apps/api",
    "build": "npm run build --workspace apps/web && npm run build --workspace apps/api",
    "lighthouse": "lhci autorun"
  },
  "devDependencies": {
    "@lhci/cli": "^0.15.1"
  }
}
